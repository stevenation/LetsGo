import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  logoContainer: {
    height: 200,
  },
  inputContainer: {
    height: 60,
  },
  input: {},
});
